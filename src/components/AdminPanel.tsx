"use client"; import {useEffect,useState} from "react"; import {getSupabaseBrowserClient} from "@/lib/supabase/browser";
type P={id:string;title:string;status:string;city:string|null;service_type:string|null;updated_at:string};
export function AdminPanel(){const[allowed,setAllowed]=useState<boolean|null>(null);const[projects,setProjects]=useState<P[]>([]);const[msg,setMsg]=useState('');useEffect(()=>{(async()=>{try{const s=getSupabaseBrowserClient();const{data:{user}}=await s.auth.getUser();if(!user){location.href='/connexion';return;}const{data:profile}=await s.from('profiles').select('role').eq('id',user.id).single();if(profile?.role!=='admin'){setAllowed(false);return;}setAllowed(true);const{data,error}=await s.from('projects').select('id,title,status,city,service_type,updated_at').order('updated_at',{ascending:false});if(error)throw error;setProjects(data||[]);}catch(e){setMsg(e instanceof Error?e.message:'Chargement impossible')}})()},[]);if(allowed===null)return <div className="dashboard-state">Vérification de l’accès…</div>;if(!allowed)return <main className="account-page"><div className="shell narrow"><h1>Accès réservé</h1><p>Cette page est réservée à l’administration.</p></div></main>;return <main className="admin-page"><div className="shell"><p className="eyebrow">Administration</p><h1>Pilotage des projets</h1><p className="page-lead">Vue privée des dossiers clients. Les actions avancées pourront être enrichies sans modifier l’architecture.</p>{msg&&<p className="form-message">{msg}</p>}<div className="admin-stats"><div><strong>{projects.length}</strong><span>projets</span></div><div><strong>{projects.filter(p=>!['completed','archived'].includes(p.status)).length}</strong><span>actifs</span></div><div><strong>{projects.filter(p=>p.status==='to_review').length}</strong><span>à vérifier</span></div></div><div className="project-list">{projects.map(p=><article className="project-row" key={p.id}><div><span className="status-pill">{p.status}</span><h2>{p.title}</h2><p>{[p.service_type,p.city].filter(Boolean).join(' · ')}</p></div><time>{new Date(p.updated_at).toLocaleDateString('fr-FR')}</time></article>)}</div></div></main>}
