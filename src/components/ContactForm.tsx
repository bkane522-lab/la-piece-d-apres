"use client";
import { FormEvent, useState } from "react"; import { getSupabaseBrowserClient } from "@/lib/supabase/browser";
export function ContactForm(){const[first,setFirst]=useState("");const[last,setLast]=useState("");const[email,setEmail]=useState("");const[phone,setPhone]=useState("");const[subject,setSubject]=useState("");const[message,setMessage]=useState("");const[status,setStatus]=useState("");const[busy,setBusy]=useState(false);
async function submit(e:FormEvent){e.preventDefault();setBusy(true);setStatus("");try{const s=getSupabaseBrowserClient();const{error}=await s.from('contact_requests').insert({first_name:first,last_name:last,email,phone:phone||null,subject:subject||null,message});if(error)throw error;setStatus("Votre message a bien été envoyé.");setMessage("");}catch(e){setStatus(e instanceof Error?e.message:"Envoi impossible.");}finally{setBusy(false)}}
return <form className="form-card" onSubmit={submit}><div className="form-grid"><label>Prénom<input required value={first} onChange={e=>setFirst(e.target.value)}/></label><label>Nom<input required value={last} onChange={e=>setLast(e.target.value)}/></label></div><label>Email<input type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Téléphone<input value={phone} onChange={e=>setPhone(e.target.value)}/></label><label>Objet<input value={subject} onChange={e=>setSubject(e.target.value)}/></label><label>Message<textarea rows={6} required value={message} onChange={e=>setMessage(e.target.value)}/></label>{status&&<p className="form-message">{status}</p>}<button className="button-primary" disabled={busy}>{busy?"Envoi…":"Envoyer"}</button></form>}
